package com.practica.clon_mocky.enums;

/**
 * Enum que representa los métodos HTTP soportados para los endpoints mockeados.
 * Cubre los métodos más utilizados en APIs REST.
 */
public enum HttpMetodo {
    GET,
    POST,
    PUT,
    DELETE,
    PATCH,
    HEAD,
    OPTIONS
}
