package com.practica.clon_mocky.enums;

/**
 * Enum que define los roles disponibles en el sistema.
 * ROLE_ADMIN: Administrador con acceso total (gestión de usuarios, ver todos los mocks).
 * ROLE_USER: Usuario normal que gestiona sus propios mocks y proyectos.
 */
public enum Rol {
    ROLE_ADMIN,
    ROLE_USER
}
